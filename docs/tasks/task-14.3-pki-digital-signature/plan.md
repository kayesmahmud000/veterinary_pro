# Task 14.3 Execution Plan: PKI Cryptographic Digital Signature (RSA-SHA256)

## 1. Prerequisites

- Task 14.1 (Structured prescription editor) and Task 14.2 (Food safety withdrawal alerts) completed and verified.
- `CryptoModule`, `PrescriptionRepository`, `ConsultationRepository`, and `PrismaService` available.

---

## 2. Implementation Steps

### Step 1: Database Schema & Migration
- [x] Add `licenseNumber String? @map("license_number") @db.VarChar(100)` to `VetProfile` in `apps/api/prisma/schema.prisma`.
- [x] Create migration SQL script `20260920240000_add_vet_license_number/migration.sql`.
- [x] Regenerate Prisma Client (`pnpm --filter @vetralink/api db:generate`).
- [x] Update `VetProfileEntity` to include `licenseNumber`.

### Step 2: Shared Types & DTO Contracts (`packages/shared-types`)
- [x] Update `packages/shared-types/src/dto/consultations/prescription.dto.ts`:
  - Add `SignPrescriptionDto` (`licenseNumber?: string`).
  - Add `CanonicalPrescriptionPayload`.
  - Add `SignatureVerificationResultDto`.
- [x] Rebuild `@vetralink/shared-types` (`pnpm --filter @vetralink/shared-types build`).

### Step 3: PKI Cryptographic Service (`PkiCryptoService`)
- [x] Create `IPkiCryptoService` in `apps/api/src/common/crypto/pki-crypto.service.interface.ts`:
  - `canonicalize(payload: Record<string, unknown>): string`
  - `hash(data: string): string`
  - `sign(data: string): string`
  - `verify(data: string, signature: string, publicKey?: string): boolean`
  - `getPublicKey(): string`
  - Token: `PKI_CRYPTO_SERVICE`
- [x] Implement `PkiCryptoService` in `apps/api/src/common/crypto/pki-crypto.service.ts`:
  - Deterministic recursive canonical JSON sorting.
  - SHA-256 hashing.
  - RSA-SHA256 signing and verification.
  - Resilient keypair initialization (environment or generated RSA-2048).
- [x] Register and export `PkiCryptoService` in `apps/api/src/common/crypto/crypto.module.ts`.

### Step 4: Service Layer & Signing Workflow
- [x] Update `IPrescriptionService` in `apps/api/src/modules/consultations/services/prescription.service.interface.ts`:
  - Add `signPrescription(consultationId: string, author: JwtPayload, dto?: SignPrescriptionDto, traceId?: string): Promise<PrescriptionDto>`
  - Add `verifyPrescriptionSignature(consultationId: string): Promise<SignatureVerificationResultDto>`
- [x] Implement `signPrescription` and `verifyPrescriptionSignature` in `apps/api/src/modules/consultations/services/prescription.service.ts`:
  - Validate attending vet/admin authorization.
  - Enforce `DRAFT` status check.
  - Fetch animal, consultation, and vet details.
  - Construct canonical prescription payload.
  - Sign with `PkiCryptoService`.
  - Call `prescriptionEntity.sign(signature)` and save to repository.
  - Emit `PRESCRIPTION_DIGITALLY_SIGNED` audit log.

### Step 5: REST Controller & Module Wiring
- [x] Update `PrescriptionsController` in `apps/api/src/modules/consultations/controllers/prescriptions.controller.ts`:
  - Add `POST /consultations/:id/prescription/sign` endpoint.
  - Add `GET /consultations/:id/prescription/verify-signature` endpoint.

### Step 6: Unit & Integration Tests Verification
- [x] Write `pki-crypto.service.spec.ts`.
- [x] Update `prescription.service.spec.ts` with signing and signature verification test cases.
- [x] Update `prescriptions.controller.spec.ts` with sign and verify-signature endpoint test cases.
- [x] Run test suite (`pnpm --filter @vetralink/api test pki prescription`).
- [x] Run full build (`pnpm --filter @vetralink/api build`).

---

## 3. Verification & Acceptance Criteria

- [x] Deterministic canonical JSON stringification produces identical hashes for identical payloads regardless of key insertion order.
- [x] RSA-SHA256 signature generated by private key is mathematically verifiable with the public key.
- [x] Tampering with any payload attribute (e.g. changing medication dosage from 10ml to 20ml) causes verification to fail.
- [x] Signed prescription transitions to `SIGNED` status with `signedAt` and `digitalSignatureHash` populated.
- [x] Subsequent attempts to edit or re-sign the prescription are rejected.
- [x] Full build succeeds with zero TypeScript errors.
